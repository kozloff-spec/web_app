function addToCart(productName, productPrice) {
    alert(`${productName} has been added to your cart for $${productPrice}`);
    // Implement further functionality for adding to cart if needed
}
